#!/usr/bin/env node
'use strict';

console.error('scripts/apply-submit.js has been retired. Use `node scripts/auto-apply-cli.js apply --job=<job-id>` for the reviewed CLI workflow.');
process.exit(1);
